<?xml version="1.0" encoding="utf-8"?>
<!-- Compact widget layout: temperature + condition only, for smaller sizes -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="8dp"
    android:background="@drawable/widget_bg"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:gravity="center">

    <!-- Location -->
    <TextView
        android:id="@+id/widget_location"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Loading..."
        android:textSize="10sp"
        android:textColor="#8a8070"
        android:textStyle="bold"
        android:fontFamily="monospace"
        android:maxLines="1"
        android:ellipsize="end"
        android:gravity="center" />

    <!-- Icon + Temp row -->
    <LinearLayout
        android:id="@+id/widget_temp_row"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="2dp">

        <TextView
            android:id="@+id/widget_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="22sp"
            android:includeFontPadding="false"
            android:layout_marginEnd="4dp" />

        <TextView
            android:id="@+id/widget_temp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="--"
            android:textSize="32sp"
            android:textColor="#0f0e0c"
            android:textStyle="bold"
            android:includeFontPadding="false"
            android:maxLines="1" />

        <TextView
            android:id="@+id/widget_temp_unit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="°"
            android:textSize="14sp"
            android:textColor="#8a8070"
            android:fontFamily="monospace" />
    </LinearLayout>

    <!-- Condition -->
    <TextView
        android:id="@+id/widget_condition"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="--"
        android:textSize="10sp"
        android:textColor="#c4411c"
        android:textStyle="italic"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginTop="1dp" />

    <!-- Hi/Lo -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="2dp">

        <TextView
            android:id="@+id/widget_hi"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="↑ --°"
            android:textSize="10sp"
            android:textColor="#c4411c"
            android:fontFamily="monospace" />

        <TextView
            android:id="@+id/widget_lo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="  ↓ --°"
            android:textSize="10sp"
            android:textColor="#1c5dc4"
            android:fontFamily="monospace" />
    </LinearLayout>

    <!-- Updated -->
    <TextView
        android:id="@+id/widget_updated"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Updated --:--"
        android:textSize="7sp"
        android:textColor="#8a8070"
        android:fontFamily="monospace"
        android:gravity="center"
        android:layout_marginTop="3dp"
        android:letterSpacing="0.1" />

</LinearLayout>
